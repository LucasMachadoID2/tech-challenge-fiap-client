apiVersion: v1
kind: Service
metadata:
  name: client-ms
  labels:
    app: client-ms
spec:
  type: ClusterIP
  selector:
    app: client-ms
  ports:
    - name: http
      port: 80
      targetPort: 8080
